<?xml version="1.0" encoding="utf-8"?>
<com.hive.views.widgets.UIRoundCornerConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/schedule_view_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:clickable="true"
    android:tag="ScriptDialog"
    app:rcv_radius="16dp"
    app:rcv_borderWidth="0.5dp"
    app:rcv_borderColor="#14FFFFFF"
    android:background="@color/design_bg_card">

    <FrameLayout
        android:id="@+id/layout_header"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        app:layout_constraintTop_toTopOf="parent">

        <TextView
            android:id="@+id/tv_time_title"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginStart="20dp"
            android:gravity="center_vertical"
            android:text="@string/time_alarm_setting_title"
            android:textColor="@color/textColorPrimary"
            android:textSize="17sp"
            android:textStyle="bold" />

        <ImageView
            android:id="@+id/iv_close"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_gravity="end"
            android:layout_marginEnd="4dp"
            android:padding="16dp"
            android:src="@drawable/icon_close"
            android:tint="@color/textColorPrimary" />
    </FrameLayout>

    <View
        android:id="@+id/view_divider"
        android:layout_width="match_parent"
        android:layout_height="0.5dp"
        android:background="@color/design_border_subtle"
        app:layout_constraintTop_toBottomOf="@id/layout_header" />

    <com.hive.timer.widget.schedule.ScheduleTimerLayout
        android:id="@+id/schedule_view"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/view_divider"
        app:layout_constraintBottom_toBottomOf="parent" />

</com.hive.views.widgets.UIRoundCornerConstraintLayout>
